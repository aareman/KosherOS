[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kosheradmin"
version = "0.1.0"
description = "KosherOS admin app — profiles, apps, updates, guardian"
readme = "README.md"
requires-python = ">=3.11"
license = "GPL-3.0-or-later"
dependencies = ["kosherd"]

[project.scripts]
kosher-admin = "kosheradmin.app:main"

[tool.hatch.build.targets.wheel]
packages = ["src/kosheradmin"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# So `pytest` works from this directory as it does from `just test`.
pythonpath = ["src", "../kosherd/src"]
