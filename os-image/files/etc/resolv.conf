# Kosher Linux: all name resolution goes through the local filtering
# resolver (kosher-dns.service). Managed statically; NetworkManager and
# systemd-resolved are configured not to touch this file.
nameserver 127.0.0.1
options trust-ad
