# What the boot splash says while this unit starts (kosheros.script shows
# the latest message it is given). The leading "-" makes a splash that is
# not running — a text boot, a serial console — harmless.
[Service]
ExecStartPre=-/usr/bin/plymouth display-message --text="Welcome. One moment…"
