# KosherOS Setup

The first-boot wizard: creates the administrator account, optionally sets
the guardian password and a boot-menu password, and explains the firmware
steps KosherOS cannot enforce itself. Runs once — kosherd refuses these
calls after setup completes.
